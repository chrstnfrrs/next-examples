import React, { ReactElement } from 'react';

import AppMenu from '../components/AppMenu';

const Home = (): ReactElement => {
  return (
    <div>
      <AppMenu />
    </div>
  );
};

export default Home;
